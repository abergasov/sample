### local run
```shell
# поднять локальный сервер
yarn dev 
cd .. && cd hardhat-smartcontract-lottery-fcc
# поднять локлаьную ноду, задеплоит контракт и обновит abi и адрес контракта в constants
yarn hardhat node 
```
